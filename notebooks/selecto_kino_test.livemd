# Selecto Kino Test

## Setup

```elixir
System.cmd("ls", ["-ld", "/data/chris/projects/selecto_test/vendor/selecto_kino"])
```

First, let's install the necessary dependencies.

```elixir
    Mix.install([
      {:kino, "~> 0.7.0"},
      {:postgrex, ">= 0.0.0"},
      {:selecto, path: "/data/chris/projects/selecto_test/vendor/selecto"},
      {:selecto_kino, path: "/data/chris/projects/selecto_test/vendor/selecto_kino"}
    ])
```

## Connect to the Database

Now, let's connect to the `selecto_test_dev` database. The form below will prompt you for the connection details. Fill in the form and click "Connect".

```elixir
SelectoKino.connect()
```

## Build a Query

Once you have connected to the database (submitted the form above), you can use the form below to build and run a query.

```elixir
SelectoKino.query()
```
