{
  "repository": {},
  "description": " ",
  "license": "MIT",
  "scripts": {
    "deploy": "NODE_ENV=production tailwind build --minify && esbuild js/app.js --bundle --minify --outdir=../priv/static/assets/ --external:/fonts/* --external:/images/*"
  },
  "dependencies": {
    "phoenix": "^1.7.14",
    "phoenix_html": "^4.1.1", 
    "phoenix_live_view": "^1.1.4"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}